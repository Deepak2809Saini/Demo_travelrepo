// Function to scroll to the content section
function scrollToContent() {
    var element = document.querySelector('.details'); // Selecting the element with class 'details'
    element.scrollIntoView({ behavior: 'smooth', block: 'start' }); // Scrolling to the content section smoothly
}

// Function to scroll to the first destination details section
function scrollToFirstSpan() {
    var element = document.querySelector('.firstspan'); // Selecting the element with class 'firstspan'
    element.scrollIntoView({ behavior: 'smooth', block: 'start' }); // Scrolling to the first destination details section smoothly
}

// Function to scroll to the second destination details section
function scrollToSecondSpan() {
    var element = document.querySelector('.secondspan'); // Selecting the element with class 'secondspan'
    element.scrollIntoView({ behavior: 'smooth', block: 'start' }); // Scrolling to the second destination details section smoothly
}

// Function to scroll to the Zanskar destination details section
function scrollToZanskar() {
    var element = document.querySelector('.Zanskar'); // Selecting the element with class 'Zanskar'
    element.scrollIntoView({ behavior: 'smooth', block: 'start' }); // Scrolling to the Zanskar destination details section smoothly
}

// Function to scroll to the "Explain" section
function scrollToExplain() {
    var element = document.getElementById('explain'); // Selecting the "Explain" section by its ID
    element.scrollIntoView({ behavior: 'smooth', block: 'start' }); // Scrolling to the "Explain" section smoothly
}
// Function to scroll to the "container" section
function scrollToContainer() {
    var element = document.getElementById('Container'); // Selecting the "container" section by its ID
    element.scrollIntoView({ behavior: 'smooth', block: 'start' }); // Scrolling to the "container" section smoothly
}


